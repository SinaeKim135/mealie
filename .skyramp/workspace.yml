services:
  - serviceName: mealie
    testDirectory: tests
    language: python
    framework: pytest
    runtimeDetails:
      serverStartCommand: "bash .skyramp/start-mealie.sh"
      runtime: local
    api:
      baseUrl: http://localhost:9000
      authHeader: Authorization
      authType: bearer
